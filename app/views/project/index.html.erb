<div class="container-fluid">
<div class = "row justify-content-md-center">
<div class = "col col-md-6">
<div class="card">
  <div class="card-header">
    Projects
    <%= link_to 'Create', new_project_path , :class => "btn btn-primary float-right" %>
    </div>
  <div class="card-body">
  <% @projects.each do |project|%>
        <%=link_to project.name,project_path(project.name) %><br/>
    <% end %>
  </div>
</div>
</div>
</div>

</div>